//stack(Primitive)   heap(Non-Primitive)

//string interpolation 
const  name = "sangu"
const repoCount = 10 
console.log(`Hello i am ${name} and my repo ccount is ${repoCount}`) // use backticks ``

const gameName = new String ('Sangu') //another way of declaring strings 
console.log(gameName[0])

console.log(gameName.__proto__)

//functions used with string 

 